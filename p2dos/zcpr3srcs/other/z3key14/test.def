;comments begin with a semicolon
;the key words ATTENTION,LEADIN,DELAY,EXPANSION,FILE, and CASE set the
;corresponding values. They should come first in the file otherwise they
;default to the present values in Z3KEY.RCP. If the CASE is ON the keys being
;defined will be upcased before being put in the table.
;
;Control characters are preceeded by a '^'. If you want to enter a '^' in
;a definition use an imbedded control^ instead. It will be converted to a '^'
;by the compiler.

ATTENTION=^]
LEADIN=\
DELAY=0
EXPANSION=2
FILE=PASKEY
CASE=off
\w=write(
\W=writeln(
\r=read(
\R=readln(
\a=At(
\^_=carat underline
^M=^M
\p=procedure 
\f=function 
=caret
\t=turbo^MY
\b=begin^Mend;^A^A^M
\u=repeat^muntil ;^h

\P=program 

